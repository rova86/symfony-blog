{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edition d'une catégorie</h1>

    {# ouverture de la balise form #}
    {{ form_start(form) }}

    {# affichage des champs du formulaire avec label #}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}

    <div class="text-right">
        <button type="submit" class="btn btn-primary">
            Enregistrer
        </button>
        <a href="{{ path('app_admin_category_index') }}" class="btn btn-outline-secondary">
            Retour
        </a>
    </div>

    {# fermeture de la balise form #}
    {{ form_end(form) }}
{% endblock %}

